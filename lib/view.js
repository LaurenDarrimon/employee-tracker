

// define functions to run based on input to see table display of company data


const viewDepartments = () => {
    
    console.log("fxn to view departments is running");

  
};

const viewRoles =  () => {
    //display formatted tabel with job title, role id, the department 
    //that role belongs to, and the salary for that role
    console.log("fxn to view roles is running");

};

const viewEmployees = () => {
    //a formatted table showing employee data, including employee ids, 
    //first names, last names, job titles, 
    //departments, salaries, and managers that the employees report to
    console.log("fxn to view employees is running");

};

module.exports = { viewDepartments, viewRoles, viewEmployees };

           